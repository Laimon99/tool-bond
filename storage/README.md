# Local storage

This folder stores temporary artifacts only when `PERSISTENCE_BACKEND` is set
to `local_file`. The public default is in-memory persistence.
`storage/local/` is intentionally git-ignored except for `.gitkeep`.
