[project]
name = "bondfx-cloudflare"
version = "0.2.0"
description = "Cloudflare Workers deployment for the BondFX public demo"
requires-python = ">=3.13"
dependencies = [
  "fastapi",
  "jsonschema",
  "openpyxl",
  "python-multipart",
]

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
